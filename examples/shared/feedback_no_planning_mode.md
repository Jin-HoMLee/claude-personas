---
name: Never use Planning Mode
description: User has banned Planning Mode (ExitPlanMode/EnterPlanMode) due to excessive usage cost
type: feedback
---
Never enter Planning Mode (do not call EnterPlanMode/ExitPlanMode tools).

**Why:** User reports Planning Mode burns up too much usage on their plan.

**How to apply:** For any task — even large/complex ones — skip plan mode entirely. If alignment is needed before implementation, propose the approach inline in 2-3 sentences and wait for user confirmation, rather than entering a formal plan mode.
