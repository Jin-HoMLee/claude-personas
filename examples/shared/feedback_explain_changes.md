---
name: Explain changes before making them
description: Always briefly explain why each code change is being made before executing the edit
type: feedback
---
Before every Edit/Write tool call, include a one-sentence explanation of WHY the change is being made (not just what it does).

**Why:** User rejected a silent edit with "always explain shortly why we do something."
**How to apply:** One sentence before each edit is enough — e.g. "Adding `import os` so `_worker_init` can set env vars." Apply in all coding sessions.
